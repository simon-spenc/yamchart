name: local-postgres
description: Local PostgreSQL for development
type: postgres

config:
  host: localhost
  port: 5432
  database: dashbook_dev

# No auth block = uses PGUSER/PGPASSWORD env vars (or defaults to postgres)
