name: local-postgres
description: Local PostgreSQL for development (via docker-compose)
type: postgres

config:
  host: localhost
  port: 5432
  database: yamchart_test

# No auth block = uses PGUSER/PGPASSWORD env vars (or defaults to postgres)
# For docker-compose, use: PGUSER=postgres PGPASSWORD=postgres
